rootProject.name = "library-bom-test"

dependencyResolutionManagement {
    versionCatalogs {
        create("libs") {
            library("library-bom", "dev.mbo", "library-bom").version("1.0.0")
        }
    }
}